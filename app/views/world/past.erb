<div class="container-fluid">
    <div clas="past">
    <div class="row">
        <div class="col-xs-6">
            <form action="/world/write/<%= @post_id %>" method="POST">
            Title : <input type="text" name="title" required>
            content : <input type="text" name="content" required>
            <input type="submit" value="작성">
            </form> 
        </div>
        <div class="col-xs-6">
            <% @post.each do |p| %>
             제목: <%= p.title %><br>
             내용: <%= p.content %>
             <a href="/world/delete/<%= p.id %>">삭제</a><br>
             ----<br>
             <% end %>
        </div>
    </div>
    </div>
</div>
